#include "main.h"

/**
 * swap_int - swap the values of two integers
 * @b : first pointer
 * @a : second pointer
 *return: void
 */

void swap_int(int *a, int *b)
{
	int costant = *a;
	*b = *a;
	*a = constant;
}
